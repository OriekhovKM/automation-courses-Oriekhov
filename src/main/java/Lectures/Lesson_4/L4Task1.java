package Lectures.Lesson_4;

import java.util.Random;

public class L4Task1 {
    public static void main(String[] args) {
        // Random = new Random();
        // int intPositive = random.nextInt(bound:100);
        // describeAge(intPositive);
        // }
        // privat static void describeAge (intPositive) {
        //  intPositive <= 12 ? "You;re kid" :
        //          intPositive <= 17 ? "Y're teaneger" :
        //                 intPositive <= 64 ? "Y're adult" : "Y're eldery";
        // }


    }
}
